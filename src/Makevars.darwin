# macOS-specific Makevars
# Uses Apple's Accelerate framework (vecLib) for BLAS/LAPACK
# No Fortran runtime needed - Accelerate is optimized native implementation

PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS)
