# Linux/Unix-specific Makevars
# Uses standard BLAS/LAPACK (typically Fortran-compiled)
# Requires FLIBS for Fortran runtime libraries

PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
