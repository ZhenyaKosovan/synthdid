% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solver.R
\name{sc.weight.fw.covariates}
\alias{sc.weight.fw.covariates}
\title{Joint Frank-Wolfe and gradient solver with covariates}
\usage{
sc.weight.fw.covariates(
  Y,
  X = array(0, dim = c(dim(Y), 0)),
  zeta.lambda = 0,
  zeta.omega = 0,
  lambda.intercept = TRUE,
  omega.intercept = TRUE,
  min.decrease = 0.001,
  max.iter = 1000,
  lambda = NULL,
  omega = NULL,
  beta = NULL,
  update.lambda = TRUE,
  update.omega = TRUE
)
}
\arguments{
\item{Y}{Collapsed outcome matrix (controls first, pre-treatment first).}

\item{X}{Array of covariates aligned with \code{Y}, shaped \code{N x T x C}.}

\item{zeta.lambda, zeta.omega}{Ridge penalties for \code{lambda} and
\code{omega}.}

\item{lambda.intercept, omega.intercept}{Logical flags for whether to demean
when fitting each set of weights.}

\item{min.decrease}{Minimum squared-improvement threshold to continue
iterating.}

\item{max.iter}{Maximum number of iterations.}

\item{lambda, omega, beta}{Optional initial values; defaults are uniform
weights and zero coefficients.}

\item{update.lambda, update.omega}{Logical flags indicating which weights to
update.}
}
\value{
A list with estimated \code{lambda}, \code{omega}, \code{beta}, and
the vector of objective values \code{vals}.
}
\description{
Alternates Frank-Wolfe updates for \code{lambda} and \code{omega} with
gradient steps for covariate coefficients \code{beta}. Supports holding
either weight vector fixed by setting \code{update.lambda} or
\code{update.omega} to \code{FALSE}.
}
\keyword{internal}
